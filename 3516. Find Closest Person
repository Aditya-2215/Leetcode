class Solution:
    def findClosest(self, x: int, y: int, z: int) -> int:
        dz=abs(x-z)
        dy=abs(y-z)
        if dz<dy:
            return 1
        elif dz>dy:
            return 2
        else:
            return 0


